line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "W", "I", "UP", "B", "C4", "SIM"]

[lint.per-file-ignores]
# Imports after the sys.path bootstrap are intentional.
"build/build.py" = ["E402"]
"tests/conftest.py" = ["E402"]
